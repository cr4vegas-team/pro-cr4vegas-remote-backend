export enum CONFIG {
    APP = 'app',
    APP_PORT = 'app.port',
    APP_GLOBAL_PREFIX = 'app.global_prefix',
    APP_JWT_SECRET = 'app.jwt_secret',
    DATABASE = 'database',
    MULTER_DEST = 'multer_dest',
}